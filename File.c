#include <stdio.h>
int main()
{
    FILE *ptr = NULL;
    // ****** Reading a file ******
    // char string[64];
    // ptr = fopen("my file.txt", "r");
    // fscanf(ptr, "%s", string);
    // printf("The content of this file has %s\n",string);
    // ****** Writing a file ******
    // char string[64] = "This content was produced by subham : ";
    // ptr = fopen("my file.txt", "w");
    // fprintf(ptr, "%s", string);
    // ****** opening a file in 'a' mode******
    // char string[64] = "This content was produced by subham : ";
    // ptr = fopen("my file.txt", "a");
    // fprintf(ptr, "%s", string);
    return 0;
}
